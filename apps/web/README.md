# BuildFlow Web

Client-facing PWA built with SvelteKit.

## Features

- Responsive design (mobile-first)
- Offline support via service worker
- Camera access for photo uploads
- Installable as PWA

## Setup

```bash
npm install
npm run dev
```
